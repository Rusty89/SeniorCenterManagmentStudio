export class Involvement {
    activityID: string; // It should be id of an activity
    memberIDs: string[]; // It should be list of members
    //memberNames: string[]; // names for the member IDs
    id?: string;
}