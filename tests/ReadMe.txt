To run the selenium test project you must install the web based selenium IDE and run it from there.

Also you must be logged into the Senior Center website prior to running the tests as the 
website has a login, and that cannot be automated with selenium.